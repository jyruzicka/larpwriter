= simple_form_for [@larp, @pc] do |f|
  = f.input :name, autofocus: true
  = f.input :description, input_html: { class: "redactor" }
  = f.association :player, collection: @larp.players.by_name, label_method: :name_with_pc_names

  = f.button :submit, class: "btn btn-info"
